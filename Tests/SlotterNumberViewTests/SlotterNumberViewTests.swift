import XCTest
@testable import SlotterNumberView

final class SlotterNumberViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(SlotterNumberView().text, "Hello, World!")
    }
}
